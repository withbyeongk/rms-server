spring:
  profiles:
    active: test

  datasource:
    url: jdbc:mariadb://3.37.31.81:3306/nutsdb?characterEncoding=UTF-8&serverTimezone=UTC
    username: iron
    password: iron
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
#      ddl-auto: none
    properties:
      hibernate:
  #      show_sql: true  # sysout으로 출력
        format_sql: true
        use_sql_comments: true  # 실행되는 쿼리에 주석 달린다.
        highlight_sql: true

  logging.level:
    org.hibernate.SQL: debug  # log로 출력
#    org.hibernate.type: trace
